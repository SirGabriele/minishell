((echo lala && (ls -la || echo non)) | echo non | ls) || echo oui

					                           	     /	\

								
(echo lala && (ls -la || echo non)) | echo non | ls								|| echo oui

							/							\

(echo lala && (ls -la || echo non)) | echo non				 ls	

						/						\
						
	echo lala && (ls -la || echo non)			echo non
	
				/			\
				
	echo lala			&& (ls -la || echo non)
	
								/				\
								
							ls -la				|| echo non
