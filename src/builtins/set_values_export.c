#include "../../includes/minishell.h"

/*t_env_ms	*set_values_export(char **content, t_env_ms *env)
{
	env = change_value_if_key_exists(content, env);
	if (!env)
		perror(NULL);
	env = extend_env_list(content);
	if (!tmp_env)
		perror(NULL);
	return (new_env);
}*/
